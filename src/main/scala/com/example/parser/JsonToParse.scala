package com.example.parser

case class JsonToParse(
                        anno: Int,
                        mese: Int,
                        giorno: Int,
                        ora : Int
                      )
